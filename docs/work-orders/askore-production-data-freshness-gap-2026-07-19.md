# Askore 운영 데이터 최신성 격차 — 2026-07-19

상태: `review_needed` — 읽기 전용 점검이며 DB·API·콘텐츠·배포 변경은 하지 않았다.

## 확인된 사실

- 대시보드 인벤토리는 Askore를 Next.js·Vercel·외부 API·DB 수집 증거가 있는 `scheduled-db-ingestion` 사이트로 분류한다.
- 로컬 `D:\web\askorekr\local.db`는 읽기 전용으로 확인했다.
  - `plants`: 8건, 최신 `updated_at`은 `2026-05-22T12:28:58Z`
  - `plant_content`: 0건
  - `care_guides`: 0건
  - `publish_queue`: 비어 있음
  - 최신 성공 파이프라인은 `2026-05-08T14:23:44Z`
- 공개 `https://askore.kr/sitemap.xml`은 `200`이며 식물 URL 474개, 블로그 URL 308개를 노출한다.
  - 공개 식물 URL의 최신 `lastmod`는 `2026-05-22T12:32:11Z`다.

## 판정

공개 운영 콘텐츠 규모와 로컬 DB의 8개 식물/0개 콘텐츠 상태가 크게 다르다. 로컬 DB는 운영 DB의 완전한 복제본이 아니거나 최신 동기화본이 아니다. 따라서 다음 행동은 금지한다.

- 로컬 `local.db`만 보고 새 콘텐츠가 없다고 결론 내리기
- 로컬 DB에서 운영 DB로 동기화하기
- 로컬 데이터로 콘텐츠 생성·발행·백필 수행하기

## 운영 DB 읽기 전용 확인

운영 Turso/LibSQL에 읽기 전용 SQL 요청을 보냈다. URL·토큰은 출력하지 않았고, 집계 결과만 확인했다.

- `plants`: 474건, 최신 `updated_at`은 `2026-05-22T12:32:11Z`, 최신 `created_at`은 `2026-05-19T23:24:49Z`
- `blog_posts`: 893건 중 공개 308건, 최신 `updated_at`은 `2026-07-19T05:00:47Z`, 최신 `created_at`은 `2026-06-21T18:21:17Z`
- `plant_content`: 0건
- `care_guides`: 0건
- `publish_queue`: 비어 있음
- 최신 성공 파이프라인: `2026-05-19T09:59:13.055Z`

공개 식물 URL 474개와 운영 DB 식물 474건은 일치하고, 공개 블로그 URL 308개와 운영 DB의 공개 `blog_posts` 308건도 일치한다. 따라서 로컬 DB의 8건은 단순히 오래된 개발 사본이었으며, 운영 DB는 수집·블로그 데이터를 모두 보유한다. 다만 식물 API 수집은 5월 이후 멈췄고, `plant_content`·`care_guides`·발행 대기열은 비어 있다.

## 다음 판단

Askore에 지금 식물 기반 콘텐츠를 대량 생성하거나 배포하지 않는다. 먼저 식물 수집 파이프라인이 5월 이후 왜 실행되지 않았는지 읽기 전용으로 추적한다. GitHub CLI로 현재 원격 저장소의 Actions 워크플로/실행 이력을 조회하려 했으나 `lsk7209/planty-friends` API가 404를 반환해, 이 환경의 GitHub 권한 또는 원격 연결 상태로는 실행 이력을 확인할 수 없었다. 새 API 데이터가 확인되더라도 수집 정체 원인이 확인되기 전에는 수집·동기화·배포를 시작하지 않는다.

## 실행 이력 판정

운영 `pipeline_runs`를 읽기 전용으로 전수 집계했다.

- 총 기록은 성공 3건뿐이다: `etl:kfri:layer1` 1건(2026-05-08), `etl:nibr:layer1` 1건(2026-05-08), `etl:nongsaro-garden:layer1` 1건(2026-05-19)
- 가장 최근 Nongsaro 수집은 입력 217건·출력 217건·거부 0건으로 성공했다.
- 실패 실행, `quality_gate_failures`, `lint_violations`는 모두 0건이다.
- 로컬 워크플로는 매일 02:00 UTC에 Nongsaro ETL을 실행하도록 선언돼 있다.

따라서 현재 근거는 ETL 코드 실패보다 **2026-05-19 이후 예약 실행이 운영 DB에 전혀 기록되지 않은 실행 공백**을 지지한다. GitHub Actions 원격 이력을 이 환경에서 읽을 수 없으므로, 다음 외부 점검은 GitHub 저장소의 워크플로 활성화·기본 브랜치 반영·비밀값 접근 권한을 확인하는 것이다. 이 점검 전에는 수동 ETL 실행이나 스케줄 수정으로 대체하지 않는다.
