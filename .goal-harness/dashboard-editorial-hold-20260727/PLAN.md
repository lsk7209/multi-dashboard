# Plan

1. Record the failed strict-chain/smoke evidence and inspect the status classifier.
2. Add the smallest classification repair and regression test.
3. Run focused tests, typecheck, lint, build, regenerate the read-only chain, and rerun smoke.
4. Review the scoped diff before the separately authorized deployment step.
