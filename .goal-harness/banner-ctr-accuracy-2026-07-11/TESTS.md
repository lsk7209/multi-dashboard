# Tests

- Unit: session token validation, automation classification, qualified aggregation, low-sample rules.
- API: image/click routes preserve valid tokens and exclude invalid or automated events.
- Regression: existing banner management store tests.
- Build: targeted lint, type-check, production build.
- Live: representative Next.js and WordPress installations emit qualified events and console shows them.
